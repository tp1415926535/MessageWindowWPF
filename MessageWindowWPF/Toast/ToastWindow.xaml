<Window x:Class="MessageWindowWPF.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MessageWindowWPF"
        mc:Ignorable="d"
        Title="ToastWindow" Background="Transparent" Topmost="True"
        AllowsTransparency="True" WindowStyle="None" SizeToContent="Height" ShowInTaskbar="False"
        MinHeight="110" Width="360" Loaded="Window_Loaded" Closing="Window_Closing">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"/>
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Storyboard x:Key="ShowWindowStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>
        <Storyboard x:Key="HideWindowStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"  From="1" To="0" Duration="0:0:0.2"/>
        </Storyboard>
        <Style TargetType="Image">
            <Setter Property="Stretch" Value="UniformToFill"/>
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <Trigger Property="Source" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ItemsControl">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Items.Count, RelativeSource={RelativeSource Self}}" Value="0">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" Opacity="0.7">
            <Grid.Effect>
                <DropShadowEffect Opacity="0.5" ShadowDepth="0" BlurRadius="10"/>
            </Grid.Effect>
        </Grid>
        <StackPanel Margin="1">
            <Image Height="180" Source="{Binding HeadImage}" Opacity="0.9"/>
            <StackPanel Margin="20">
                <Grid Margin="0,0,0,10" >
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
                        <Image Source="{Binding Icon}" Height="15" Width="15" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Title}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                        <TextBlock Text="{Binding Time,StringFormat='yyyy-MM-dd HH:mm:ss'}" Opacity="0.2" Margin="0,0,8,0" FontSize="11" VerticalAlignment="Center"/>
                        <Button Width="15" Height="15" VerticalAlignment="Center" Click="CloseButton_Click"
                                Background="Transparent" Cursor="Hand" BorderThickness="0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type Button}">
                                                <Border Background="{TemplateBinding Background}">
                                                    <ContentPresenter  Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Effect" Value="{x:Null}"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect ShadowDepth="0" BlurRadius="8" Color="LightGray" />
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <Path Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"  StrokeThickness="1"
                                  Stretch="Uniform" Margin="2" Opacity="0.5" Data="M0,0 1,1 M1,0 0,1"/>
                        </Button>
                    </StackPanel>
                </Grid>
                <TextBlock Text="{Binding Header}" FontWeight="Black" FontSize="15" Margin="0,5"/>
                <ItemsControl ItemsSource="{Binding Contents}" Margin="0,5">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Opacity="0.7" Margin="0,2"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Image Height="180" Source="{Binding BodyImage}" Opacity="0.9" Margin="0,10,0,0"/>
                <ItemsControl ItemsSource="{Binding Buttons}" Margin="-3,10,-3,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="1"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Text}" Click="EventButton_Click"
                                    Margin="3,5,3,0" Padding="0,7" FontSize="13" Cursor="Hand" BorderThickness="0" Opacity="0.8" FontWeight="Black"
                                    Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
